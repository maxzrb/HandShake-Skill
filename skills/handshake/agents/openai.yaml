name: HandShake
description: Multi-agent project continuity workflow for Codex, Claude Code, Cursor, Gemini CLI, OpenCode, and similar coding agents using one mandatory AI-facing STATUS log plus Chinese progress/version records, with built-in update guidance for the HandShake skill itself.
metadata:
  default_prompt: "Use HandShake proactively for non-trivial repository work, continuation, status checks, tool/device/environment switches, multi-agent handoff across Codex/Claude/Cursor/Gemini/OpenCode, session closeout, and requests to update or upgrade the HandShake skill itself. Read AGENTS.md, CLAUDE.md or GEMINI.md when relevant, docs/codex/INDEX.md, and docs/codex/STATUS.md. Treat STATUS.md as the single AI-facing source of truth. For substantial work, update the current snapshot, append a YYYY-MM-DD HH:MM session log entry at the end of STATUS.md, append a matching Chinese progress entry to version/工作进度.md, update version/版本迭代记录.md only for version/release changes, and report git status. For installed skill updates, prefer update_installed_skill.py --latest --all --force unless the user provides a zip or source path."
