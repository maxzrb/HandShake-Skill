# 版本迭代记录

用途：面向中文用户记录版本发布、重要变更、影响范围、验证结果和迁移说明。仅当项目版本或发布状态变化时更新。

## 当前版本

- 版本号：2.1.0-beta
- 发布日期：2026-06-08
- 状态：本地多平台适配 beta，尚未提交/打 tag。
- 主要变化：补齐仓库根层 agent 入口；新增 Cursor plugin manifest、Codex/OpenCode 安装说明、Gemini 入口、共享 Python session-start hook；扩展 Claude plugin metadata；更新 README 和 skill 元数据；修复安装脚本的 source package 定位逻辑。
- 影响范围：HandShake skill 仓库结构、安装说明、plugin metadata、`skills/handshake/` 与 `handshake/` 包镜像。
- 验证情况：镜像同步、JSON 校验、Python 编译、hook 输出、安装定位测试、`git diff --check` 已通过；`quick_validate.py` 因当前环境缺少 `PyYAML` 未完成。
- 已知风险：Claude/Cursor plugin runtime 尚未实际加载验证；需要补跑 skill validator。
- 迁移说明：目标项目记录模型没有变化；安装时仍应使用 `handshake/` 或 `skills/handshake/`，不要把仓库根层兼容 `SKILL.md` 当作完整 skill 包安装。

## 历史版本

发布新版本时，先把旧的“当前版本”内容移到本节顶部，再写入新的“当前版本”。不要删除历史记录。
