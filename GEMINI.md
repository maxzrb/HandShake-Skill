@./skills/handshake/SKILL.md

This repository develops the HandShake multi-agent continuity skill.

Use `AGENTS.md` for repository maintenance rules. Treat `skills/handshake/` as the authoritative package and `handshake/` as the Codex install mirror.
